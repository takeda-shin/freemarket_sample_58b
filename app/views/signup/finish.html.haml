.signup-login-wrapper

  - if session[:password_confirmation].nil?
    = render partial: "signup-header-finish"
  - else
    = render partial: "social-login-finish"

  .signup-main
    .signup-box
      .signup-box__title
        %h2 会員登録完了
      .signup-box__form
        = link_to root_path, method: :get, class: "signup-box__form__btn signup-box__form__start" do
          メルカリをはじめる

  = render partial: "common-footer" 