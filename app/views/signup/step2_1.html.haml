.signup-login-wrapper

  = render partial: "signup-header-step2"

  .signup-main
    .signup-box
      .signup-box__title
        %h2 電話番号の確認
      .signup-box__form
        .signup-box__form__content
          = form_for @user, url: step2_1_signup_index_path, method: :post, html: { class: "field-wrapper" } do |f|
            .field
              .field__label
                = f.label :携帯電話の番号, class: 'field__label__title'
              .field__input
                = f.text_field :phone_number, class: 'field__input__default',placeholder: "携帯電話の番号"
                %p 本人確認のため、携帯電話のSMS(ショートメッセージサービス)を利用して認証を行います。
              = f.submit "SMSを送信する", class: "submit-btn"
              %p ※電話番号は本人確認や不正利用防止のために利用します。他のユーザーに公開されることはありません。
              .field__right
                = link_to "#" do
                  電話番号の確認が必要な理由〉

  = render partial: "common-footer" 